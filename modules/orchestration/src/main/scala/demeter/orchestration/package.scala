package demeter

/** 08-orchestration — daily run, degradation, observability, config
  *
  * See `specs/` in the repo root for the spec-driven contracts this module
  * implements. Each unit spec maps to one or more source files here, and its
  * Gherkin scenarios map to the tests under src/test.
  */
package object orchestration
